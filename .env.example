# Copy to .env. Credentials must never be committed.
OPENAI_API_KEY=
# Choose an available model with image input and structured output support.
ATLAS_READING_MODEL=
# Blank defaults to this project's data/ directory.
ATLAS_DATA_DIR=
